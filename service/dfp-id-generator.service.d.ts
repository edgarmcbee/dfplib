export declare class DfpIDGeneratorService {
    private generatedIDs;
    generateID(type?: string): any;
    dfpIDGenerator(element: HTMLElement): any;
    isTaken(id: any): boolean;
    isUnique(id: any): boolean;
}
